<% if status.road.length %>
* <small>OSM length: <%= '%.2f' % status.road.length %> km ([<%= create_osmonitor_url(status.road) %> show on map])</small>
<% else %>
* <small>OSM length: <b>unknown</b> ([<%= create_osmonitor_url(status.road) %> show on map])</small>
<% end %>
