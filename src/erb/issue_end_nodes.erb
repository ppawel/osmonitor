<% if !issue.data[:too_few].empty? %>
* <small>some components have too few end nodes (<% issue.data[:too_few].each {|end_nodes| %><%= end_nodes.size %>, <% } %>)</small>
<% end %>
<% if !issue.data[:too_many].empty? %>
* <small>some components have too many end nodes (<% issue.data[:too_many].each {|end_nodes| %><%= end_nodes.size %>, <% } %>)</small>
<% end %>
