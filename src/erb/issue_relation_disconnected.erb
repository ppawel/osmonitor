<% if status.road.has_roles %>
* <small>relation disconnected: [<%= status.backward_url %> backward] (<%= status.backward_components.size %>), [<%= status.forward_url %> forward] (<%= status.forward_components.size %>)</small>
<% else %>
* <small>relation disconnected: [<%= status.all_url %> all] (<%= status.all_components.size %>)</small>
<% end %>
<% if !status.backward_fixes.empty? %>
* <small>potential backward problems: <% status.backward_fixes.each {|way| %>{{way|<%= way.id %>}} <% } %>
<% end %>
<% if !status.forward_fixes.empty? %>
* <small>potential forward problems: <% status.forward_fixes.each {|way| %>{{way|<%= way.id %>}} <% } %>
<% end %>
