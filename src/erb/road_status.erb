<b>Info:</b>
<% if status.road.relation %>
* <small>OSM length: <%= status.road.get_osm_length %> km</small>
* <small>ways with {{tag|lanes}}: <%= status.road.percent_with_lanes %>%</small>
* <small>ways with {{tag|maxspeed}}: <%= status.road.percent_with_maxspeed %>%</small>
<% end %>

<b>Warnings:</b>
<% if !status.road.relation %>
* <small>no relation</small>
<% elsif !status.connected %>
* <small>relation disconnected (<%= status.components %>)</small>
<% end %>
<% if status.road.has_many_covered_relations %>
* <small>has more than one relation: <% status.road.other_relations.each { |rel| %><%= rel['id'] %>; <% } %>
<% end %>
<% if status.road.relation and !status.road.has_proper_network %>
* <small>relation has wrong {{tag|network}} value (is <tt><%= status.road.get_network %></tt>, should be <tt><%= status.road.get_proper_network %></tt>)
<% end %>
<% if status.road.relation and !status.road.has_proper_length.nil? and !status.road.has_proper_length %>
* <small>length difference is too large (<%= status.road.length_diff %> km)</small>
<% end %>
